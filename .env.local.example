# Backend agent microservice the UI proxies to. Optional locally — defaults to
# the deployed backend (see lib/backend.js). For local dev against a local backend:
# BACKEND_URL=http://localhost:8080
BACKEND_URL=https://autonomous-growth-engine-backend.onrender.com
